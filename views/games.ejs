<style>
  /* Defines gradient background and ensures text is centered */
  body {
      font-family: Verdana, sans-serif;
      background: linear-gradient(to bottom, rgb(255, 222, 173), rgb(173, 216, 230));
      margin: 0;
      padding-top: 80px;
      text-align: center;
      min-height: 100vh;
      box-sizing: border-box;
  }

  /* Container for hover sliding navigation bar */
  .header-hover-area {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      z-index: 1000;
  }

  /* Animates and styles dropping navigation bar */
  .sliding-nav-container {
      width: 100%;
      background-color: rgb(255, 218, 185);
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.15);
      padding: 10px 0;
      transform: translateY(0);
      transition: none;
  }

  /* Defines margins and text alignment for the navigation bar */
  .sliding-nav-container nav {
      margin: 5px;
      text-align: center;
  }

  /* Styles unhovered navigation links */
  .sliding-nav-container nav a {
      display: inline-block;
      margin: 0 20px;
      text-decoration: none;
      color: rgb(0, 102, 204);
      font-size: 2.5em;
      transition: color 0.3s ease, transform 0.3s ease;
  }

  /* Transitions navigation links to different color when hovered */
  nav a:hover {
      color: rgb(255, 105, 180);
      transform: scale(1.1);
      text-decoration: underline;
  }

  /* Enables the dropdown bar to... drop down */
  .header-hover-area:hover .sliding-nav-container {
      transform: translateY(0);
  }

  /* Defines header "Games List" */
  h1 {
      color: rgb(72, 61, 139);
      margin-top: 0;
      margin-bottom: 20px;
      font-size: 6em;
  }
  /* Styling for outer card */
  .game-card {
    background-color: rgb(255, 239, 213);
    border: 2px solid rgb(157, 189, 232);
    border-radius: 15px;
    padding: 15px;
    margin: 40px auto;
    max-width: 27.5em;
    text-align: center;
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
}
/* Styling for inner card */
.game-card a {
    display: inline-block;
    font-size: 2em;
    text-decoration: none;
    padding: 10px 20px;
    background-color: rgb(240, 248, 255);
    border-radius: 10px;
    transition: background-color .6s, transform .6s;
}
/* Transition for inner card when mouse hovers over */
.game-card a:hover {
    background-color: rgb(255, 182, 193);
    transform: scale(1.2);
}

</style>

<div class="header-hover-area">
  <div class="hover-trigger">
  </div>

  <div class="sliding-nav-container">
    <nav>
      <a href="/">Back to Home</a>
    </nav>
  </div>
</div>

<h1>Games List</h1>

<div class="game-card">
  <a href="/games/matching">Play Matching Game</a>
</div>


</html>